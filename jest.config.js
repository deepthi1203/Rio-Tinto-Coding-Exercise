module.exports = {
    preset: 'ts-jest',
    testEnvironment: 'node',
    moduleFileExtensions: ['ts', 'js'],
    collectCoverage: true,
    verbose: true,
    coveragePathIgnorePatterns: ['mock']
  };
  